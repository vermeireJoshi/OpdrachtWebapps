let Request = require('request');

describe("server",()=>{
	beforeAll(() => {});
	
	it("first test",()=>{
		expect(15).toBe(15);
	});
});

describe("API ",() => {
    it("second test", () => {
        expect(15).toBe(15);
    });
});